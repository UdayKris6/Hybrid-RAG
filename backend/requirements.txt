# Fast API framework for building the REST endpoints
fastapi>=0.100.0
# ASGI server for running the FastAPI application
uvicorn>=0.22.0
# Official OpenAI SDK to generate text embeddings and call GPT models
openai>=1.0.0
# Python client to communicate with the Qdrant vector database
qdrant-client>=1.7.0
# Library to run local neural networks for reranking and text embeddings
sentence-transformers>=2.2.2
# Helper to read environment variables from a .env file
python-dotenv>=1.0.0
# Data validation and settings management using python type annotations
pydantic>=2.0.0
